import React from "react"
import Person from "./Person"

export default function PersonList (props) {
    console.log(props);

    // const mappedPeople = props.people.map((element, index) => {
    //     return <h1></h1>
    // });

    return <div className="person-list">Kenobi</div>
}